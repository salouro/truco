package test;

import java.util.concurrent.Semaphore;

import javax.swing.JFrame;

import controller.ControlPartida;
import model.Baralho;
import model.Carta;
import model.Jogador;
import model.PC;
import model.Partida;
import model.Pessoa;
import model.Turno;
import view.jogo.JogoPrincipal;

public class TesteJogoPrincipal {

	public static void main(String[] args) {
	/*	Jogador jogador = new Pessoa("teste");
		Turno turno = new Turno();
		Baralho baralho = new Baralho();
		PC pc = new PC("pc");
		Partida partida = new Partida();
		
		ControlPartida cp = new ControlPartida(jogador, baralho, partida, turno, pc, new Semaphore(1));
		JogoPrincipal jogo = new JogoPrincipal(jogador);
		jogo.setVisible(true);
		jogo.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		
		cp.run();
		jogador.setCartaJogada(new Carta());*/
	}

}
